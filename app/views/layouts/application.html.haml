%head
  %title ApprenticeshipsNorfolk
  = csrf_meta_tags
  = stylesheet_link_tag    'application', media: 'all'
  = javascript_include_tag 'application'


%body

  .viewport
    = render 'header'
    .main.main--hasborder
      = render 'sidebar'
      .main__cell
        .content
          - if content_for? :breadcrumbs
            = breadcrumbs(yield :breadcrumbs)
          = yield
    = render 'footer'





  :javascript
    $('.learner_dob').datepicker({
    dateFormat: 'dd/mm/yy',
    minDate: '#{$age_date_limit}',
    changeMonth: true,
    changeYear: true
    });
