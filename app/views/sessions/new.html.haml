= provide :breadcrumbs, 'I have already applied for a grant and need to upload evidence to claim'

#pre_content
  .typography

    %h1 PLEASE LOGIN TO THE SYSTEM USING THE PASSWORD YOU CREATED
    %p
      Please complete the following steps below to register and complete an application for funding. Once you are logged in the next steps will guide you through the upload process.
      %br
      For answers to any questions you may have, please see links to the left of the screen.

    %h2
      %strong 1. Please complete the following details.

.searchform    
  .typography
    = form_for :session, url: login_path do |f|
      - if flash.any?
        %p#MemberLoginForm_LoginForm_error.message.bad
          - flash.each do |k, v|
            = v
      .field
        = f.label :email
        = f.email_field :email, placeholder: "Email"
      .field
        = f.label :password
        = f.password_field :password, placeholder: "Password"
      .actions
        = link_to "Forgot password?", new_password_reset_path
        = f.submit "Log in"
